# 주차 단속 시스템 - DLL OCR 버전 (2.0V)
# Python 의존성 패키지

# 웹 프레임워크
flask>=2.0.0
waitress>=2.0.0

# 이미지 처리
opencv-python>=4.5.0
numpy>=1.20.0
Pillow>=9.0.0

# 데이터 처리
pandas>=1.3.0
openpyxl>=3.0.0

# 객체 탐지 (YOLO, YOLO26 지원)
ultralytics>=8.4.0

# HTTP 요청
requests>=2.25.0

# 빌드 도구 (개발용)
pyinstaller>=5.0.0
